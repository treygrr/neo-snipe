node_modules/
dist/
dist-safari/
dist-firefox/
release/
.dev-profile*/

# Xcode's derived data for the Safari app: 180MB of module caches and
# intermediates, all rebuildable. The generated project beside it is tracked.
safari/build/
# Per-developer Xcode state, if Xcode is opened on the project.
xcuserdata/
*.xcuserstate

# Scratch icon art, not used by the build
extension/icons/image.png

# Ad-hoc probe scripts
inspect-neo.mjs
hover-probe.mjs
opts-probe.mjs
